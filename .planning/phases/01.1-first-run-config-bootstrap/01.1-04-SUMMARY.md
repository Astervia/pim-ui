---
phase: 01.1-first-run-config-bootstrap
plan: 04
subsystem: ui
tags: [error-banner, daemon-state, react, crash-on-boot, brand]

requires:
  - phase: 01.1-02
    provides: DaemonLastError discriminated union + isCrashOnBoot/pickCrashOnBoot helpers + CrashOnBootError shape
  - phase: 01.1-01
    provides: sidecar Terminated-within-500ms detection routed through daemon://state-changed (RPC error.data with kind=crash_on_boot)
  - phase: 01-04
    provides: LimitedModeBanner Phase-1 baseline with 5 existing variants and the action-row JSX

provides:
  - LimitedModeBanner.variantFor() gains a crash-on-boot branch keyed on `pickCrashOnBoot(lastError)` that returns the D-20 destructive variant with verbatim body `pim-daemon exited in {elapsed_ms} ms during startup. Check config at {path}.`
  - Sub-row glyph `·› {stderr_tail_first_line}` rendered as a nested `<span>` in muted-foreground / font-code / text-xs (U+00B7 + U+203A literal — NOT the ASCII variants)
  - `suppressActions: true` on the crash variant collapses the START / RETRY / VIEW LOGS button row per D-20 (Phase 3 owns the re-edit flow, never Phase 01.1)
  - The Variant interface gains two NEW optional fields (`subRow?: string`, `suppressActions?: boolean`); existing 5 variants leave both undefined and continue to render the action row
  - `variantFor()` signature widened from `errorMsg: string | null` to `lastError: DaemonLastError | null` so the crash branch can read `path` / `elapsed_ms` / `stderr_tail` directly off the union without re-parsing RpcError.data

affects: [phase-03 (owns the re-edit / re-bootstrap flow that this banner explicitly does NOT trigger)]

tech-stack:
  added: []
  patterns:
    - "Specific-before-generic branch ordering inside `variantFor()` — crash check runs first so the D-20 body wins over the otherwise-correct `state === 'error'` body when both are true (sidecar Terminated transitions DaemonState to error AND emits the crash payload)"
    - "Optional Variant fields with `undefined` default — additive interface extension that does not touch the 5 existing variants' rendering paths"

key-files:
  created: []
  modified:
    - src/components/brand/limited-mode-banner.tsx
    - src/screens/first-run.tsx  # disabled-radio polish (commit 5949a3b)

key-decisions:
  - "Crash branch is FIRST in variantFor() (above all existing branches) — `state === 'error'` is also true during a crash because sidecar Terminated transitions DaemonState::Error before emitting; without the order, the generic destructive body would render and the path/elapsed_ms would never surface"
  - "`suppressActions` is a flag on Variant rather than removing the button JSX — the Phase 1 / Phase 2 button path stays intact for every other error case (handshake timeout, daemon-not-found); only the crash variant suppresses"
  - "`stderrFirstLine = crash.stderr_tail.split('\\n')[0] ?? ''` — when stderr_tail is empty, subRow is undefined and the `·›` line does not render (no orphaned glyph)"
  - "No Retry button in the crash variant per D-20 — the user is led to the Settings raw-TOML editor (Phase 3) to fix the broken config, not to a Retry button that would just respawn a daemon doomed to die again"
  - "Sub-row uses existing tokens only: `text-xs text-muted-foreground font-code` — no new design tokens, no new components, brand-discipline-clean"

patterns-established:
  - "Banner extension via Variant flags — future variants can add optional rendering switches (subRow, suppressActions) without forking variantFor or duplicating render JSX"
  - "Type-narrowed payload lift — variantFor takes the full DaemonLastError union and uses `pickCrashOnBoot()` to lift the crash payload, rather than passing a pre-extracted message string. Pattern repeats across discriminated-union consumers in the app."

requirements-completed:
  - SETUP-03

duration: ~7 min (commit 09949a3) + ~3 min radio polish (commit 5949a3b)
completed: 2026-04-26
---

# Phase 01.1 Plan 04: limited-mode-banner-crash-on-boot Summary

**Closed the SETUP-03 loop: when pim-daemon dies within 500 ms of spawn, LimitedModeBanner now shows the verbatim D-20 copy citing the config path + elapsed_ms + first stderr line — and explicitly NO Retry button, because Phase 3 owns the re-edit flow.**

## Performance

- **Duration:** ~7 min (Task 1 single feat commit) + ~3 min radio polish
- **Started:** 2026-04-26
- **Completed:** 2026-04-26
- **Tasks committed:** 1 of 2 (Task 2 was the human-verify checkpoint — see Notes)
- **Files modified:** 1 (banner) + 1 (radio polish, technically a Plan 01.1-03 follow-up but committed in the same window)

## Accomplishments
- Crash-on-boot variant branches FIRST in `variantFor()`, returning the D-20 verbatim body with template-literal interpolation of `crash.elapsed_ms` + `crash.path` and a `·› {first stderr line}` sub-row.
- Variant interface extended with two optional fields (`subRow?: string`, `suppressActions?: boolean`) — additive, every existing variant leaves both undefined.
- `suppressActions === true` collapses the `[ START DAEMON ] / [ RETRY START ] / [ VIEW LOGS ]` row per D-20. The 5 existing variants continue to render their action rows untouched.
- `variantFor()` signature widened from `errorMsg: string | null` to `lastError: DaemonLastError | null`; existing branches derive `errorMsg` locally via the rpc_error variant (no behavioral change).
- Imports added: `pickCrashOnBoot` + `DaemonLastError` from `@/lib/daemon-state`; `RpcError` from `@/lib/rpc-types`.
- Disabled-role radio polished (commit 5949a3b): inline "(Linux only — unavailable)" suffix, ⊘ glyph, opacity-45, stronger selected-state visual on macOS / Windows.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add the C1 crash-on-boot variant branch (variantFor extension + suppressActions + subRow)** — `09949a3` (feat)
2. **Task 2: Final human-verify checkpoint (all 6 ROADMAP Phase 01.1 SCs)** — closed retroactively (see Notes)

Adjacent polish (technically Plan 01.1-03 follow-up, same execution window):

3. **Disabled-role radio: macOS/Windows clarity (D-08 polish)** — `5949a3b` (fix)

## Files Created/Modified
- `src/components/brand/limited-mode-banner.tsx` (modified) — Variant interface extended (`subRow?`, `suppressActions?`); `variantFor()` signature changed to accept `DaemonLastError | null`; new crash branch placed first; render JSX gains conditional `<span>` for sub-row + ternary suppression of action row.
- `src/screens/first-run.tsx` (modified, commit 5949a3b) — disabled-role radio gets the `(Linux only — unavailable)` inline suffix + ⊘ glyph + opacity-45 to make it unmistakable on macOS / Windows. Adjacent polish, not strictly Plan 01.1-04 scope, but landed in the same window.

## Cross-Phase Invariants

W1 single-listener contract verified post-plan:
- `grep -c "listen(" src/lib/rpc.ts` returns `0`
- `grep -c "listen(" src/hooks/use-daemon-state.ts` returns exactly `2`

No new event channel anywhere:
- `grep -rc "daemon://crash-on-boot" src/` returns `0`
- `grep -rc "daemon://crash-on-boot" src-tauri/src/` returns `0`

D-20 verbatim copy present in limited-mode-banner.tsx:
- `pim-daemon exited in` — present
- `during startup. Check config at` — present
- `·›` (U+00B7 + U+203A) — present
- `pickCrashOnBoot` consumed — present
- `suppressActions` — present (interface field + render-path conditional)
- `subRow` — present (interface field + render-path use)

All Phase 1 banner variants preserved (regression check):
- `LIMITED MODE` — present
- `STARTING DAEMON` — present
- `RECONNECTING` — present
- `DAEMON STOPPED UNEXPECTEDLY` — present

## Notes

The Task 2 human-verify checkpoint (running the app from a clean config, walking SC-1 through SC-6 against a fresh `pnpm tauri dev` launch) and the SUMMARY artifact were never formally written during the original execution window. SUMMARY backfilled here on 2026-04-27 as part of the Phase 3 pre-flight cleanup — the user confirmed at that point that the runtime walk had been done previously and all 6 Phase 01.1 ROADMAP success criteria pass against the current build. No code changes were required to close this plan; only the bookkeeping artifact was missing.

End-to-end Phase 01.1 outcome:
- Plan 01.1-01: Rust foundation (config_path, default_config, bootstrap_config + config_exists Tauri commands, sidecar crash-on-boot routed through daemon://state-changed) — closed.
- Plan 01.1-02: TS contract (bootstrapConfig + configExists wrappers + DaemonLastError discriminated union + isCrashOnBoot/pickCrashOnBoot helpers) — closed.
- Plan 01.1-03: UI (AppRoot router + FirstRunScreen + useConfigBootstrap boot-sequence hook + main.tsx wire-in) — closed.
- Plan 01.1-04: LimitedModeBanner crash-on-boot variant — closed here.

W1 invariant preserved across all 4 plans (post-phase listen counts: 0 + 2).
