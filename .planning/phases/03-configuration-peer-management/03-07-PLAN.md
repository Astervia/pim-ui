---
phase: 03-configuration-peer-management
plan: 07
type: execute
wave: 5
depends_on:
  - "03-02"
  - "03-03"
  - "03-04"
  - "03-05"
  - "03-06"
complexity: small
files_modified: []
autonomous: false
requirements:
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
  - CONF-06
  - CONF-07
  - PEER-02
  - PEER-03
  - OBS-02
  - OBS-03

must_haves:
  truths:
    - "User verifies, by running the live Tauri app against a real pim-daemon, that all six ROADMAP Phase 3 success criteria hold simultaneously"
    - "W1 single-listener contract is preserved across the whole phase: grep -c 'listen(' src/hooks/use-daemon-state.ts returns 2; grep -rn 'from \"@tauri-apps/api/event\"' src/ | grep -v use-daemon-state.ts returns empty"
    - "Every 11-requirement ID (PEER-02, PEER-03, CONF-01..07, OBS-02, OBS-03) has been exercised by the user through a live UI interaction (checked off the criterion list below)"
    - "Brand discipline across all new Phase 3 files: zero rounded-md/lg/full/xl, zero hex-literal Tailwind colors (bg-green-[0-9], bg-red-[0-9], bg-blue-*, bg-purple-*, text-red-*, text-green-*)"
    - "All verbatim locked-copy strings grep positive across the codebase (one check per locked string)"
    - "No zod dependency anywhere in the codebase (D-08 negative assertion — addresses checker Warning 2): ! grep -q \"\\\"zod\\\"\" package.json AND ! grep -rn \"from \\\"zod\\\"\" src/"

  artifacts: []
  key_links: []
---

<objective>
Human-verify checkpoint: run the Phase 3 UI against a live pim-daemon and confirm all six ROADMAP success criteria hold. Also runs a grep-based audit across every Phase 3 file for W1, brand discipline, and verbatim locked copy — the final gate before declaring the phase complete.

Purpose: Automated checks in each plan verify single files. This plan verifies that the SYSTEM works end-to-end against the real daemon, which is the only way to catch regressions across plan boundaries (e.g. raw-wins flag persists but doesn't re-show after refetch; restart-required toast fires but clicking [ Restart ] doesn't restart cleanly; debug snapshot JSON is valid but doesn't download).

Output:
- No code changes
- A `03-07-VERIFICATION.md` doc capturing the six-criterion audit results, any gaps found, and a final grep-audit report
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-configuration-peer-management/03-CONTEXT.md
@.planning/phases/03-configuration-peer-management/03-UI-SPEC.md
@.planning/phases/03-configuration-peer-management/03-01-SUMMARY.md
@.planning/phases/03-configuration-peer-management/03-02-SUMMARY.md
@.planning/phases/03-configuration-peer-management/03-03-SUMMARY.md
@.planning/phases/03-configuration-peer-management/03-04-SUMMARY.md
@.planning/phases/03-configuration-peer-management/03-05-SUMMARY.md
@.planning/phases/03-configuration-peer-management/03-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated audit sweep across all Phase 3 files</name>
  <files>.planning/phases/03-configuration-peer-management/03-07-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/03-configuration-peer-management/03-UI-SPEC.md §Copywriting Contract (every locked string)
    - .planning/phases/03-configuration-peer-management/03-CONTEXT.md (D-01..D-32 for decision cross-reference)
  </read_first>
  <action>
    Run each of the automated audits below (copy-paste into a shell) and record the results in `.planning/phases/03-configuration-peer-management/03-07-VERIFICATION.md`. Any FAIL is a phase-scope blocker and MUST be reported back to the user with the failing file + line.

    ```bash
    # 1. Type + build
    pnpm typecheck
    pnpm build

    # 2. W1 single-listener contract
    grep -c 'listen(' src/hooks/use-daemon-state.ts          # MUST be 2
    grep -rn 'from "@tauri-apps/api/event"' src/ | grep -v 'src/hooks/use-daemon-state.ts' | wc -l   # MUST be 0

    # 3a. No raw-editor libs (D-14 — the plain-textarea contract)
    grep -rn "codemirror\|monaco\|prism" src/                # MUST be empty

    # 3b. No zod anywhere (D-08 — authoritative validation is the daemon; addresses checker Warning 2)
    ! grep -q '"zod"' package.json                          # MUST NOT match
    ! grep -rn 'from "zod"' src/                             # MUST NOT match

    # 4. Brand discipline sweep across ALL new Phase 3 files
    grep -rnE "(rounded-md|rounded-lg|rounded-full|rounded-xl|bg-green-[0-9]|bg-red-[0-9]|bg-blue-[0-9]|bg-purple-[0-9]|text-red-[0-9]|text-green-[0-9])" \
        src/components/settings/ src/components/peers/ src/components/logs/ \
        src/screens/settings.tsx src/screens/peers.tsx \
        src/hooks/use-add-peer.ts src/hooks/use-remove-peer.ts \
        src/hooks/use-settings-config.ts src/hooks/use-section-save.ts \
        src/hooks/use-raw-toml-save.ts src/hooks/use-section-raw-wins.ts \
        src/hooks/use-pending-restart.ts src/hooks/use-log-filters.ts \
        src/lib/debug-snapshot.ts src/lib/config/
    # MUST be empty

    # 5. Locked copy — run each one; all MUST match
    grep -r "Add a static peer" src/components/peers/
    grep -r "Peer address" src/components/peers/
    grep -r "How to reach it" src/components/peers/
    grep -r "Nickname (optional)" src/components/peers/
    grep -r "That peer is already configured." src/hooks/
    grep -r "Address format not recognized by the daemon." src/hooks/
    grep -r "This peer will be removed from pim.toml and disconnected." src/components/peers/
    grep -r "Nearby discovery can re-pair it later." src/components/peers/
    grep -r "Peer was already removed." src/hooks/
    grep -r "\\[ + Add static peer \\]" src/components/peers/
    grep -r "Raw is source of truth — form view shows a subset" src/components/settings/
    grep -r "\\[ Open Advanced \\]" src/components/settings/
    grep -r "Daemon stopped — reconnect to save." src/components/settings/
    grep -r "Reconnect to add peers." src/components/peers/
    grep -r "Reconnect to remove peers." src/components/peers/
    grep -r "search messages, sources, peers…" src/components/logs/
    grep -r "Last 5 min" src/components/logs/
    grep -r "Last 15 min" src/components/logs/
    grep -r "Last 1 hour" src/components/logs/
    grep -r "All session" src/components/logs/
    grep -r "Custom…" src/components/logs/
    grep -r "Filter by time range" src/components/logs/
    grep -r "no log rows match these filters" src/components/logs/
    grep -r "\\[ Export debug snapshot \\]" src/components/logs/
    grep -r "Snapshot saved as" src/components/logs/
    grep -r "Couldn't generate snapshot." src/components/logs/
    grep -r "Couldn't parse TOML returned by daemon." src/components/settings/
    grep -r "Device name" src/components/settings/sections/
    grep -r "Interface name" src/components/settings/sections/
    grep -r "Mesh address mode" src/components/settings/sections/
    grep -r "Listen port" src/components/settings/sections/
    grep -r "Broadcast discovery" src/components/settings/sections/
    grep -r "Bluetooth discovery" src/components/settings/sections/
    grep -r "Wi-Fi Direct discovery" src/components/settings/sections/
    grep -r "Auto-connect to discovered peers" src/components/settings/sections/
    grep -r "Authorization policy" src/components/settings/sections/
    grep -r "Allow all (trust-on-first-use disabled)" src/components/settings/sections/
    grep -r "Allow list (only peers in trusted-peers)" src/components/settings/sections/
    grep -r "Trust on first use (default for mesh discovery)" src/components/settings/sections/
    grep -r "Static — I set the address" src/components/settings/sections/
    grep -r "Automatic — pim picks an address" src/components/settings/sections/
    grep -r "Maximum hops" src/components/settings/sections/
    grep -r "All gateways lost" src/components/settings/sections/
    grep -r "Kill-switch active" src/components/settings/sections/
    grep -r "UI version" src/components/settings/sections/
    grep -r "\\[ Copy path \\]" src/components/settings/sections/
    grep -r "\\[ Open crash log \\]" src/components/settings/sections/
    grep -r "github.com/Astervia/proximity-internet-mesh" src/components/settings/sections/

    # 6. Requirement coverage — every plan has non-empty requirements field
    grep -r "^requirements:" .planning/phases/03-configuration-peer-management/*-PLAN.md
    ```

    Produce `03-07-VERIFICATION.md` with one section per check, marked PASS / FAIL. Any FAIL halts the phase.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm build && [ "$(grep -c 'listen(' src/hooks/use-daemon-state.ts)" = "2" ] && [ "$(grep -rn 'from \"@tauri-apps/api/event\"' src/ | grep -v 'src/hooks/use-daemon-state.ts' | wc -l | tr -d ' ')" = "0" ] && ! grep -rn "codemirror\|monaco\|prism" src/ && ! grep -q '"zod"' package.json && ! grep -rn 'from "zod"' src/ && test -f .planning/phases/03-configuration-peer-management/03-07-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `03-07-VERIFICATION.md` exists and has one row per audit check (types/build/W1/raw-editor libs/brand/each locked string)
    - Every row is `PASS` — any FAIL blocks the checkpoint
    - `pnpm typecheck` exit 0; `pnpm build` exit 0
    - W1: `grep -c 'listen(' src/hooks/use-daemon-state.ts` returns exactly `2`
    - Brand: sweep for `rounded-md|lg|full|xl` + hex-literal Tailwind colors across every Phase 3 directory returns empty
    - No `codemirror|monaco|prism` anywhere in `src/` (D-14 absolute)
    - Every locked-copy string from 03-UI-SPEC §Copywriting Contract matches at least once in the expected file
    - Checker Warning 2 negative assertion: `! grep -q '"zod"' package.json` AND `! grep -rn 'from "zod"' src/` — zod is NOT a dependency and NOT imported anywhere (D-08: daemon is source of truth for validation)
  </acceptance_criteria>
  <done>
    All automated audits pass; VERIFICATION.md filed; ready for the human checkpoint (Task 2).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human-verify checkpoint against all six ROADMAP Phase 3 success criteria</name>
  <files>(no files modified — human verification of running app)</files>
  <read_first>
    - .planning/ROADMAP.md — Phase 3 Success Criteria list (ground truth for the six criteria)
    - .planning/phases/03-configuration-peer-management/03-UI-SPEC.md §Copywriting Contract — every locked copy string the user verifies on screen
    - .planning/phases/03-configuration-peer-management/03-CONTEXT.md — D-01..D-32 (decisions the live behavior must match)
    - .planning/phases/03-configuration-peer-management/03-07-VERIFICATION.md (produced by Task 1)
  </read_first>
  <action>
    Hand off to the user for live verification. The executor SHOULD NOT edit any source file during this task — the app is already running via `pnpm tauri dev`. The executor's role is to:
    1. Ensure `pnpm tauri dev` is running and a real `pim-daemon` binary is present in `src-tauri/binaries/` (or guide the user to run `pnpm fetch-daemon` if missing).
    2. Walk the user through the six success-criterion sections in `<how-to-verify>` below, recording PASS / FAIL with any observed failure details.
    3. If any criterion FAILs, append a `## Gaps` section to `03-07-VERIFICATION.md` with the failing criterion, the observed behavior, and the file most likely responsible. This file is the input to `/gsd:plan-phase --gaps` for closure planning.
    4. Await the user's `approved` signal (or failure description) via the resume prompt.

    All UI interaction happens in the user's window; this task performs no `callDaemon` / file edits / commits.
  </action>
  <what-built>
    The completed Phase 3 stack:
    - Settings screen at `⌘6` with nine collapsible CliPanel sections in fixed order (Identity, Transport, Discovery, Trust, Routing, Gateway, Notifications, Advanced — raw config, About), each with a visible one-line summary when collapsed and full form when expanded (CONF-01)
    - Editable form controls for node name (CONF-02), transport knobs (CONF-03), discovery toggles (CONF-04), and trust policy radio (CONF-05) — each with a per-section `[ Save ]` that runs dry_run → real save
    - Raw TOML editor (plain textarea + gutter) with daemon-side validation, inline error markers on reject, preserved edit buffer, and `requires_restart` handling (CONF-06)
    - Raw-is-source-of-truth banner (`Raw is source of truth — form view shows a subset`) showing on any section whose TOML AST has keys the form cannot represent (CONF-07)
    - Dedicated Peers tab at `⌘2` with `[ + Add static peer ]` action row, Sheet-based form, and per-row `[ Remove ]` with confirmation AlertDialog (PEER-02, PEER-03)
    - Logs tab enhanced with debounced text search, time-range filter (five presets + Custom… dialog), and `[ Export debug snapshot ]` button downloading a JSON file (OBS-02, OBS-03)
  </what-built>
  <how-to-verify>
    ## Prerequisites
    - `pnpm install` has been run (confirms deps from 03-01 landed)
    - `pnpm fetch-daemon` has populated `src-tauri/binaries/` (Phase 1 artifact)
    - Launch: `pnpm tauri dev`

    ## Success Criterion 1 — Eight (nine) collapsible sections in fixed order with visible summaries (CONF-01)
    **Expected:** ROADMAP phrasing (after 03-01's typo fix) now reads "nine collapsible sections" matching CONTEXT D-03 + UX-PLAN §6f. Verify all nine appear.
    1. Press `⌘6`. Sidebar row `settings` turns active with `▶ ` prefix.
    2. Settings pane shows nine CollapsibleCliPanel sections in this exact order:
       IDENTITY → TRANSPORT → DISCOVERY → TRUST → ROUTING → GATEWAY → NOTIFICATIONS → ADVANCED — RAW CONFIG → ABOUT
    3. Each section is collapsed by default with its one-line summary visible in the header (e.g. Discovery shows `broadcast on · BLE off · wifi_direct off · N trusted peers`).
    4. Click any section header → body slides open (≤ 250ms); chevron swaps `▸` → `▾`.
    5. Press `⌘↑` → all sections collapse. Press `⌘↓` → all sections expand. (D-06)

    **PASS criteria:**
    - Nine sections in fixed order
    - Summaries visible while collapsed
    - Click-expand works
    - ⌘↑ / ⌘↓ collapse-all / expand-all

    ## Success Criterion 2 — Editable form controls + save (CONF-02..05)
    1. Expand IDENTITY. Device name is editable; change it. Click `[ Save ]`.
    2. Expand TRANSPORT. Change MTU. Click `[ Save ]`.
    3. Expand DISCOVERY. Flip broadcast Switch. Click `[ Save ]`.
    4. Expand TRUST. Change policy radio. Click `[ Save ]`.
    5. After each save: toast fires (`Saved.` OR `Saved. Restart pim to apply: ...`). Reload the app; values persist (daemon is source of truth).

    **PASS criteria:**
    - All four sections accept edits and save without UI errors
    - Post-save refetch shows the saved values
    - Restart-required fields trigger the `[ Restart ]` toast action

    ## Success Criterion 3 — Raw TOML editor + per-line errors + preserved buffer (CONF-06)
    1. Expand ADVANCED — RAW CONFIG. Textarea shows current TOML with line-number gutter.
    2. Edit the file to inject a deliberate error (e.g. change a numeric field to a string: `listen_port = "abc"`). Click `[ Save ]`.
    3. Daemon rejects; inline `col X: message` rows appear below erroring lines; gutter `⚠` marker appears next to the line number (text-accent color, not red).
    4. **Edit buffer is still on screen** — the text you typed has NOT been reset.
    5. Click the gutter `⚠` marker → cursor jumps to `(line, column)` in the textarea.
    6. Fix the error → `[ Save ]` → toast `Saved.` + gutter markers disappear.

    **PASS criteria:**
    - Rejected save preserves the edit buffer (no reset)
    - `⚠` gutter markers + `col X: message` inline rows render
    - Clicking marker jumps cursor to the error
    - Successful save clears the markers

    ## Success Criterion 4 — Raw-is-source-of-truth banner (CONF-07)
    1. Edit the raw TOML to add a field OUTSIDE the form schema — e.g. `[transport.advanced]` table with `reconnect_backoff_ms = 500`. Click `[ Save ]`; daemon should accept (or reject — if reject, swap for a different unmapped key).
    2. Collapse ADVANCED. Expand TRANSPORT.
    3. The TRANSPORT section body now shows the banner: `Raw is source of truth — form view shows a subset` with a `[ Open Advanced ]` button.
    4. Click `[ Open Advanced ]` → scrolls back to the Advanced section.
    5. Remove the unmapped field via the raw editor, save. Reopen TRANSPORT — banner is gone.

    **PASS criteria:**
    - Banner appears on sections whose TOML has unmapped keys
    - Banner copy is VERBATIM `Raw is source of truth — form view shows a subset`
    - `[ Open Advanced ]` scrolls to Advanced section
    - Banner disappears once the unmapped keys are removed

    ## Success Criterion 5 — Add/remove static peer reflected in list within 2s (PEER-02, PEER-03)
    1. Press `⌘2` → Peers tab shows CONNECTED + NEARBY panels.
    2. Click `[ + Add static peer ]`. Right-edge Sheet opens at 480px titled `Add a static peer`.
    3. Type an address (e.g. `192.168.1.99:9000`), select `tcp`, optional nickname `test-peer`. Click `[ Add peer ]`.
    4. Sheet closes on success. Within 2 seconds the peer appears in the CONNECTED list (or fails with honest status from daemon).
    5. On that row (if static), a `[ Remove ]` button is right-aligned. Click it.
    6. AlertDialog titled `Remove test-peer?` with body `This peer will be removed from pim.toml and disconnected. Nearby discovery can re-pair it later.` opens.
    7. Click `[ Remove ]`. Dialog closes; row disappears from the list within 2 seconds.

    **PASS criteria:**
    - Add Sheet opens with all three fields (Peer address, How to reach it, Nickname) verbatim
    - `[ Add peer ]` submits and the peer appears in the list within 2s
    - Remove AlertDialog shows the VERBATIM D-19 body
    - Remove confirms and the row disappears within 2s
    - Non-static peers (e.g. discovered peers) do NOT render `[ Remove ]`

    ## Success Criterion 6 — Logs text search + time range + debug snapshot (OBS-02, OBS-03)
    1. Press `⌘5` → Logs tab. Filter bar shows three rows: level buttons (row 1), search input (row 2), peer + time + export (row 3).
    2. Type into search — log rows filter live with a 300ms debounce. Clear the search; all rows return.
    3. Change time range to `Last 5 min`. Rows older than 5 min hide. Change to `All session`. All rows return.
    4. Change to `Custom…`. Dialog titled `Filter by time range` opens with From + To time inputs. Pick a range, click `[ Apply ]`. Rows filter accordingly.
    5. Click `[ Export debug snapshot ]`. File downloads as `pim-debug-snapshot-{ISO-with-hyphens}.json`. Open it and verify: `snapshot_version: 1`, `ui_version`, `captured_at`, `daemon_status`, `peers`, `discovered`, `logs`, `filters_applied` fields all present.

    **PASS criteria:**
    - Search input debounces 300ms and matches case-insensitive substring in `message + source + peer_id`
    - Five time-range presets work; Custom… opens the Dialog
    - Debug snapshot file downloads with the correct filename format
    - JSON contains all eight snake_case fields per D-23/D-24

    ## Gap handling
    If ANY criterion FAILs, document the failure in `03-07-VERIFICATION.md` (append a `## Gaps` section) and report back via the resume signal. `/gsd:plan-phase --gaps` will take that file and generate closure plans.

    ## Final step
    When all six criteria pass, type `approved` at the resume prompt. The phase is DONE.
  </how-to-verify>
  <verify>
    Manual human verification — no automated command. Pass/fail documented in `03-07-VERIFICATION.md` per criterion.
  </verify>
  <done>
    User types `approved` at the resume prompt after walking all six ROADMAP Phase 3 success criteria live against a real pim-daemon, OR user describes failures and the executor appends a `## Gaps` section to VERIFICATION.md.
  </done>
  <resume-signal>Type `approved` when all six ROADMAP Phase 3 success criteria verify live, OR describe any failures for gap-closure planning.</resume-signal>
</task>

</tasks>

<verification>
Checkpoint gate — user confirms all six criteria via live Tauri session against real pim-daemon.

Pre-gate automated sweep (Task 1) MUST be green before the human-verify step starts.
</verification>

<success_criteria>
- All six ROADMAP §Phase 3 Success Criteria verify LIVE against a real pim-daemon
- `03-07-VERIFICATION.md` exists with PASS/FAIL matrix
- Every verbatim locked-copy string from 03-UI-SPEC §Copywriting Contract present in the expected file
- W1 (`grep -c 'listen(' src/hooks/use-daemon-state.ts == 2`) preserved throughout
- No rounded-md/lg/full/xl, no hex-literal Tailwind colors anywhere in Phase 3 new files
- No codemirror / monaco / prism in the source tree (D-14 absolute)
</success_criteria>

<output>
After completion, create `.planning/phases/03-configuration-peer-management/03-07-SUMMARY.md` documenting:
- Audit matrix (Task 1) with PASS/FAIL per check
- Human-verify results (Task 2) — one row per ROADMAP success criterion with PASS/FAIL and observations
- Any deferred follow-ups (captured in deferred/ ideas for Phase 4/5 planners)
- Phase 3 declared complete (or a list of gaps for `/gsd:plan-phase --gaps`)
</output>
