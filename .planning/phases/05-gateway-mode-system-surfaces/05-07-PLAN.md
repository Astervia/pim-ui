---
phase: 05-gateway-mode-system-surfaces
plan: 07
type: execute
wave: 4
depends_on:
  - "05-01"
  - "05-02"
  - "05-03"
  - "05-04"
  - "05-05"
  - "05-06"
files_modified: []
autonomous: false
requirements: []
must_haves:
  truths:
    - "grep -rn 'TBD-PHASE-4-' src/ src-tauri/ surfaces ALL Phase-4 integration markers; the inventory matches RESEARCH §4 letters A through G (with E and F covered explicitly even when no source-side marker exists — E was scoped out of Phase 5, F lives in Plan 05-05 routing actions)"
    - "grep -rn 'TBD-PHASE-4-A' src/ src-tauri/ returns ≥ 3 hits (Plan 05-04 frontend + Rust + Plan 05-05 routing actions)"
    - "grep -rn 'TBD-PHASE-4-B' src/ src-tauri/ returns ≥ 2 hits (Plan 05-04 frontend selector + Rust menu)"
    - "grep -rn 'TBD-PHASE-4-C' src/ src-tauri/ returns ≥ 1 hit (Plan 05-06 use-gateway-notifications kill_switch consumer)"
    - "grep -rn 'TBD-PHASE-4-D' src/ src-tauri/ returns ≥ 1 hit (Plan 05-06 policy.ts module comment — COPY.md audit deferral)"
    - "grep -rn 'TBD-PHASE-4-G' src/ src-tauri/ returns ≥ 3 hits (Plan 05-04 frontend + Rust + Plan 05-05 routing actions + Plan 05-06 AppShell listener) — note: F (routing screen) appears in Plan 05-05 actions.ts; E (onboarding screen IDs) was scoped OUT of Phase 5 per RESEARCH §4 verdict"
    - "grep -c 'TBD-RPC' src/lib/rpc-types.ts returns ≥ 5 hits (Plan 05-01 D-37: GatewayStatusResult method shape, GatewayEventKind, GatewayEvent payload, gateway.status method registry entry, gateway.event registry entry)"
    - "grep -c 'TBD-RPC-FALLBACK' src/hooks/use-gateway-status.ts returns ≥ 1 hit (Plan 05-03 D-16 polling-fallback path)"
    - "Brand-discipline audit across all Phase-5 NEW files passes: no 'rounded-(sm|md|lg|xl|full)', no 'shadow-(sm|md|lg|xl)', no 'bg-gradient', no '#[0-9a-fA-F]{3,8}' literal hex colors, no exclamation marks in user-facing strings"
    - "W1 daemon-event invariant preserved: grep -c 'listen(' src/lib/rpc.ts returns 0; grep -c 'listen(' src/hooks/use-daemon-state.ts returns 2; the only NEW listen() is the documented pim://open-add-peer listener in app-shell.tsx (custom Tauri event, NOT a daemon event — explicit exception)"
    - "ALL six ROADMAP §Phase 5 success criteria observably pass in a running app — confirmed via the human-verify checkpoint task"
    - "ALL eight Phase-5 REQ IDs (GATE-01..04, UX-04..07) have user-facing surfaces verified end-to-end on a real pim-daemon"
  artifacts: []
  key_links: []
---

<objective>
Audit + human-verify checkpoint. Plans 05-01..06 ship the implementation; this plan confirms the Phase-5 boundary holds: every TBD-PHASE-4-* / TBD-RPC / TBD-RPC-FALLBACK marker is greppable, every brand-discipline rule passes, the W1 invariant survives, and a human walks the six ROADMAP §Phase 5 success criteria against a running `pim-daemon`.

Purpose: Phase 5 closes v1 for Mira. Without this plan we ship code without visual / functional verification on a real daemon. Pattern from Plan 02-06 + 01.1-04 (verified-on-real-daemon checkpoint).

This plan claims ZERO new requirements (`requirements: []`) — every Phase-5 REQ is already claimed by Plans 05-02..06. The audit task instead verifies the COLLECTIVE coverage end-to-end.

Output:
- `05-07-SUMMARY.md` recording grep audit results + human-verify outcome.
- No code changes. `files_modified: []`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-gateway-mode-system-surfaces/05-CONTEXT.md
@.planning/phases/05-gateway-mode-system-surfaces/05-RESEARCH.md
@.design/branding/pim/patterns/STYLE.md
@docs/UX-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep audit — TBD-PHASE-4 inventory + TBD-RPC + brand discipline + W1</name>
  <read_first>
    - .planning/phases/05-gateway-mode-system-surfaces/05-RESEARCH.md §4 (TBD-PHASE-4 inventory letters A..G), §5 (TBD-RPC type additions), §12 (validation architecture testability table)
    - .planning/phases/05-gateway-mode-system-surfaces/05-CONTEXT.md D-37 (TBD-RPC marker count ≥ 5), D-16 (TBD-RPC-FALLBACK in use-gateway-status.ts)
    - All six Plan 05-NN-PLAN.md files (read for what each was supposed to add)
    - .design/branding/pim/patterns/STYLE.md (hard-constraint list)
  </read_first>
  <action>
    Execute the following audit script. Each grep below MUST exit with the asserted condition; if not, STOP and re-open the offending plan to fix the missing marker / brand violation. The audit is automated except for the per-plan SUMMARY cross-reference (which is read-only documentation).

    **A. TBD-PHASE-4 inventory audit (per RESEARCH §4 letters A..G):**

    Run from repo root:
    ```bash
    # Per-letter counts — each must be ≥ the threshold from must_haves.truths.
    A=$(grep -rn "TBD-PHASE-4-A" src/ src-tauri/ | wc -l)
    B=$(grep -rn "TBD-PHASE-4-B" src/ src-tauri/ | wc -l)
    C=$(grep -rn "TBD-PHASE-4-C" src/ src-tauri/ | wc -l)
    D=$(grep -rn "TBD-PHASE-4-D" src/ src-tauri/ | wc -l)
    F=$(grep -rn "TBD-PHASE-4-F" src/ src-tauri/ | wc -l)
    G=$(grep -rn "TBD-PHASE-4-G" src/ src-tauri/ | wc -l)
    # E (onboarding screen IDs) was scoped OUT of Phase 5 per RESEARCH §4 verdict —
    # we expect 0 hits and this is correct.
    E=$(grep -rn "TBD-PHASE-4-E" src/ src-tauri/ | wc -l)

    echo "TBD-PHASE-4 inventory: A=$A B=$B C=$C D=$D E=$E F=$F G=$G"

    # Assertions:
    test "$A" -ge 3 || { echo "FAIL: TBD-PHASE-4-A count $A < 3"; exit 1; }
    test "$B" -ge 2 || { echo "FAIL: TBD-PHASE-4-B count $B < 2"; exit 1; }
    test "$C" -ge 1 || { echo "FAIL: TBD-PHASE-4-C count $C < 1"; exit 1; }
    test "$D" -ge 1 || { echo "FAIL: TBD-PHASE-4-D count $D < 1"; exit 1; }
    test "$F" -ge 1 || { echo "FAIL: TBD-PHASE-4-F count $F < 1"; exit 1; }
    test "$G" -ge 3 || { echo "FAIL: TBD-PHASE-4-G count $G < 3"; exit 1; }
    test "$E" -eq 0 || { echo "WARN: TBD-PHASE-4-E found ($E hits) but was scoped OUT — review."; }
    ```

    **B. TBD-RPC + TBD-RPC-FALLBACK audit (per Plan 05-01 D-37 + Plan 05-03 D-16):**

    ```bash
    RPC=$(grep -c "TBD-RPC" src/lib/rpc-types.ts)
    FALLBACK=$(grep -c "TBD-RPC-FALLBACK" src/hooks/use-gateway-status.ts)

    echo "TBD-RPC=$RPC  TBD-RPC-FALLBACK=$FALLBACK"

    test "$RPC" -ge 5 || { echo "FAIL: TBD-RPC count in rpc-types.ts < 5"; exit 1; }
    test "$FALLBACK" -ge 1 || { echo "FAIL: TBD-RPC-FALLBACK not present in use-gateway-status.ts"; exit 1; }
    ```

    **C. Brand-discipline sweep across Phase-5 NEW files:**

    The Phase-5 new files are confined to:
    - `src/screens/gateway.tsx`
    - `src/components/gateway/`
    - `src/components/tray-popover/`
    - `src/components/command-palette.tsx`
    - `src/lib/gateway/`
    - `src/lib/notifications/`
    - `src/lib/command-palette/`
    - `src/hooks/use-gateway-preflight.ts`, `use-gateway-status.ts`, `use-gateway-notifications.ts`, `use-system-notifications.ts`, `use-command-palette.ts`
    - `src/tray-popover-main.tsx`
    - `tray-popover.html`
    - `src-tauri/src/tray.rs`
    - `src-tauri/capabilities/tray-popover.json`
    - `src-tauri/icons/tray.png`

    Run brand-discipline greps:
    ```bash
    # 1. radius-0 enforcement (no rounded-* classes other than rounded-none which is implicit)
    if grep -rEn "rounded-(sm|md|lg|xl|full)" \
        src/screens/gateway.tsx \
        src/components/gateway/ \
        src/components/tray-popover/ \
        src/components/command-palette.tsx \
        src/lib/gateway/ \
        src/lib/notifications/ \
        src/lib/command-palette/ \
        src/hooks/use-gateway-preflight.ts \
        src/hooks/use-gateway-status.ts \
        src/hooks/use-gateway-notifications.ts \
        src/hooks/use-system-notifications.ts \
        src/hooks/use-command-palette.ts \
        src/tray-popover-main.tsx 2>/dev/null; then
      echo "FAIL: rounded-* class found in Phase-5 file"; exit 1
    fi

    # 2. no shadows
    if grep -rEn "shadow-(sm|md|lg|xl)" \
        src/screens/gateway.tsx \
        src/components/gateway/ \
        src/components/tray-popover/ \
        src/components/command-palette.tsx \
        src/lib/ \
        src/hooks/use-gateway-preflight.ts \
        src/hooks/use-gateway-status.ts \
        src/hooks/use-gateway-notifications.ts \
        src/hooks/use-system-notifications.ts 2>/dev/null; then
      echo "FAIL: shadow-* class found"; exit 1
    fi

    # 3. no gradients
    if grep -rEn "bg-gradient" \
        src/screens/gateway.tsx \
        src/components/gateway/ \
        src/components/tray-popover/ \
        src/components/command-palette.tsx \
        src/lib/ 2>/dev/null; then
      echo "FAIL: bg-gradient class found"; exit 1
    fi

    # 4. no hex literals (tokens only)
    if grep -rEn "#[0-9a-fA-F]{3,8}" \
        src/screens/gateway.tsx \
        src/components/gateway/ \
        src/components/tray-popover/ \
        src/components/command-palette.tsx \
        src/lib/gateway/ \
        src/lib/notifications/ \
        src/lib/command-palette/ \
        src/hooks/use-gateway-preflight.ts \
        src/hooks/use-gateway-status.ts \
        src/hooks/use-gateway-notifications.ts \
        src/hooks/use-system-notifications.ts \
        src/hooks/use-command-palette.ts 2>/dev/null; then
      echo "FAIL: hex literal found in Phase-5 file"; exit 1
    fi

    # 5. no exclamation marks in prose. Note we are scoping to .tsx + .ts files
    # whose negation operators are === false / === null per Phase 2 D-29 bang-free
    # policy — but allow ! in src/lib/notifications/policy.ts ONLY if it appears
    # inside a string template that is NOT user-facing (which would be a defect anyway).
    # Use a stricter form: look for ! immediately preceded or followed by alphabetic chars
    # in JSX-text positions or in string literals. For Phase 5 the goal is zero hits.
    if grep -rE "[\"'][^\"']*![^\"']*[\"']" \
        src/screens/gateway.tsx \
        src/components/gateway/ \
        src/components/tray-popover/ \
        src/components/command-palette.tsx \
        src/lib/gateway/ \
        src/lib/notifications/ \
        src/lib/command-palette/ 2>/dev/null | grep -v "//.*!"; then
      # Filter out TypeScript negation patterns (!== / != ) by counting only string-quoted bangs
      # (the pattern above already restricts to inside quotes). Anything that surfaces is a defect.
      echo "WARN: exclamation mark inside a string literal — review whether user-facing"
    fi
    ```

    **D. W1 daemon-event invariant audit + custom-event listener inventory:**

    ```bash
    RPC_LISTEN=$(grep -c 'listen(' src/lib/rpc.ts)
    DAEMON_LISTEN=$(grep -c 'listen(' src/hooks/use-daemon-state.ts)
    SHELL_LISTEN=$(grep -c 'listen(' src/components/shell/app-shell.tsx)
    LIB_RS_LISTEN=$(grep -c 'app\.listen' src-tauri/src/lib.rs)

    echo "W1 audit: rpc.ts=$RPC_LISTEN  use-daemon-state.ts=$DAEMON_LISTEN  app-shell.tsx=$SHELL_LISTEN  lib.rs(app.listen)=$LIB_RS_LISTEN"

    test "$RPC_LISTEN" -eq 0 || { echo "FAIL: src/lib/rpc.ts has $RPC_LISTEN listen() — W1 violated"; exit 1; }
    test "$DAEMON_LISTEN" -eq 2 || { echo "FAIL: src/hooks/use-daemon-state.ts has $DAEMON_LISTEN listen() — expected 2"; exit 1; }
    # app-shell.tsx may have 0 or 1 (the documented pim://open-add-peer custom event).
    # If > 1, a new daemon-event listener may have leaked outside W1.
    test "$SHELL_LISTEN" -le 1 || { echo "FAIL: src/components/shell/app-shell.tsx has $SHELL_LISTEN listen() — expected ≤ 1 (only pim://open-add-peer)"; exit 1; }

    # Rust setup hook owns custom Tauri-event listeners that bridge the popover
    # window into app-level actions. Plan 05-04 W1 fix adds `app.listen("pim://quit", ...)`.
    # Expected ≥ 1: the `pim://quit` listener. If a future phase adds more
    # cross-window IPC listeners, document each in the comment block below.
    test "$LIB_RS_LISTEN" -ge 1 || { echo "FAIL: src-tauri/src/lib.rs has $LIB_RS_LISTEN app.listen — expected ≥ 1 (pim://quit)"; exit 1; }
    grep -qE 'app\.listen.*"pim://quit"' src-tauri/src/lib.rs \
      || { echo "FAIL: pim://quit listener (Plan 05-04 W1 fix) is not wired in src-tauri/src/lib.rs"; exit 1; }

    # Documented custom-event listener inventory (greppable from this plan):
    # - pim://open-add-peer  src/components/shell/app-shell.tsx  (Plan 05-06)
    # - pim://quit           src-tauri/src/lib.rs                (Plan 05-04 W1 fix)
    ```

    **E. Verbatim copy-gate audit (Phase-5 verbatim strings):**

    ```bash
    # GATE-04 verbatim copy
    grep -q "Gateway mode is Linux-only today." src/components/gateway/linux-only-panel.tsx \
      || { echo "FAIL: GATE-04 verbatim string missing"; exit 1; }
    grep -q "Your device can still join a mesh as a client or relay." src/components/gateway/linux-only-panel.tsx \
      || { echo "FAIL: GATE-04 continuation string missing"; exit 1; }

    # D-09 pre-flight failure banner copy
    grep -q "Pre-flight failed — fix the items above and re-run." src/components/gateway/preflight-section.tsx \
      || { echo "FAIL: D-09 banner copy missing"; exit 1; }

    # D-15 advisory copy
    grep -q "peers will be cut over to another gateway" src/components/gateway/gateway-active-panel.tsx \
      || { echo "FAIL: D-15 advisory copy missing"; exit 1; }

    # D-14 empty-state peer-through-me copy
    grep -q "no peers routing through this node yet · advertising 0.0.0.0/0" src/components/gateway/peers-through-me-list.tsx \
      || { echo "FAIL: D-14 empty-state copy missing"; exit 1; }

    # D-34 critical toast copy
    grep -q "Blocking internet — gateway unreachable. Open pim to fix." src/lib/notifications/policy.ts \
      || { echo "FAIL: D-34 kill-switch toast copy missing"; exit 1; }
    grep -q "Mesh has no gateway — internet routing lost." src/lib/notifications/policy.ts \
      || { echo "FAIL: D-34 all-gateways-lost system copy missing"; exit 1; }
    ```

    **F. ROADMAP success-criteria → plan trace** (RESEARCH §12 + 05-CONTEXT canonical_refs):

    Output a manual cross-reference:
    ```
    SC1 (Linux pre-flight + nat_interface picker + enable)        → Plan 05-02 GATE-01, GATE-02
    SC2 (active state: gauge + throughput + peer-through-me)      → Plan 05-03 GATE-03
    SC3 (macOS / Windows Linux-only messaging)                    → Plan 05-02 GATE-04
    SC4 (tray popover + tray menu parity)                         → Plan 05-04 UX-05, UX-06
    SC5 (⌘K palette surfaces every major action)                  → Plan 05-05 UX-07
    SC6 (toast for non-critical, OS notification only for critical)→ Plan 05-06 UX-04
    ```

    **G. Sanity build:**

    ```bash
    pnpm typecheck
    cd src-tauri && cargo check
    ```

    All audits pass → proceed to Task 2 (human verify). If any audit fails, STOP and re-open the offending plan.

    Commit: `chore(05-07): grep audit — Phase-5 markers + brand discipline + W1 invariant`.

    NO file changes — this task is read-only audit. The commit is empty (or skipped) and the audit results are recorded in the SUMMARY in Task 2's Output.
  </action>
  <verify>
    <automated>test "$(grep -rn 'TBD-PHASE-4-A' src/ src-tauri/ | wc -l)" -ge 3 && test "$(grep -rn 'TBD-PHASE-4-B' src/ src-tauri/ | wc -l)" -ge 2 && test "$(grep -rn 'TBD-PHASE-4-C' src/ src-tauri/ | wc -l)" -ge 1 && test "$(grep -rn 'TBD-PHASE-4-D' src/ src-tauri/ | wc -l)" -ge 1 && test "$(grep -rn 'TBD-PHASE-4-F' src/ src-tauri/ | wc -l)" -ge 1 && test "$(grep -rn 'TBD-PHASE-4-G' src/ src-tauri/ | wc -l)" -ge 3 && test "$(grep -c 'TBD-RPC' src/lib/rpc-types.ts)" -ge 5 && test "$(grep -c 'TBD-RPC-FALLBACK' src/hooks/use-gateway-status.ts)" -ge 1 && test "$(grep -c 'listen(' src/lib/rpc.ts)" = "0" && test "$(grep -c 'listen(' src/hooks/use-daemon-state.ts)" = "2" && test "$(grep -c 'listen(' src/components/shell/app-shell.tsx)" -le 1 && test "$(grep -c 'app\.listen' src-tauri/src/lib.rs)" -ge 1 && grep -qE 'app\.listen.*"pim://quit"' src-tauri/src/lib.rs && grep -qE 'Image::from_path.*tray\.png' src-tauri/src/lib.rs && grep -q '"icons/tray.png"' src-tauri/tauri.conf.json && ! grep -q "exit as appExit" src/components/tray-popover/popover-actions.tsx && grep -q "Gateway mode is Linux-only today." src/components/gateway/linux-only-panel.tsx && grep -q "Your device can still join a mesh as a client or relay." src/components/gateway/linux-only-panel.tsx && grep -q "Pre-flight failed — fix the items above and re-run." src/components/gateway/preflight-section.tsx && grep -q "no peers routing through this node yet · advertising 0.0.0.0/0" src/components/gateway/peers-through-me-list.tsx && grep -q "Blocking internet — gateway unreachable. Open pim to fix." src/lib/notifications/policy.ts && grep -q "Mesh has no gateway — internet routing lost." src/lib/notifications/policy.ts && pnpm typecheck && (cd src-tauri && cargo check) && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - **TBD-PHASE-4 inventory audit:**
      - `grep -rn "TBD-PHASE-4-A" src/ src-tauri/` returns ≥ 3 hits (Plan 05-04 frontend tbd-route-toggle + Rust tray.rs route_toggle + Plan 05-05 actions.ts route on/off TBD-PHASE-4-A handlers)
      - `grep -rn "TBD-PHASE-4-B" src/ src-tauri/` returns ≥ 2 hits (Plan 05-04 frontend use-route-status-line + Rust tray.rs route_status)
      - `grep -rn "TBD-PHASE-4-C" src/ src-tauri/` returns ≥ 1 hit (Plan 05-06 use-gateway-notifications kill_switch consumer)
      - `grep -rn "TBD-PHASE-4-D" src/ src-tauri/` returns ≥ 1 hit (Plan 05-06 policy.ts module comment)
      - `grep -rn "TBD-PHASE-4-F" src/ src-tauri/` returns ≥ 1 hit (Plan 05-05 routing-table action.run + nav.routing handler)
      - `grep -rn "TBD-PHASE-4-G" src/ src-tauri/` returns ≥ 3 hits (Plan 05-04 frontend popover-actions + Rust tray.rs add_peer + Plan 05-05 peers.add_nearby + Plan 05-06 AppShell listener)
    - **TBD-RPC marker audit:**
      - `grep -c "TBD-RPC" src/lib/rpc-types.ts` returns ≥ 5 (D-37)
      - `grep -c "TBD-RPC-FALLBACK" src/hooks/use-gateway-status.ts` returns ≥ 1 (D-16)
    - **W1 invariant + custom-event listener inventory:**
      - `grep -c 'listen(' src/lib/rpc.ts` returns `0`
      - `grep -c 'listen(' src/hooks/use-daemon-state.ts` returns `2`
      - `grep -c 'listen(' src/components/shell/app-shell.tsx` returns `≤ 1` (only pim://open-add-peer)
      - `grep -c 'app\.listen' src-tauri/src/lib.rs` returns `≥ 1` — Rust setup hook owns custom-event listeners (Plan 05-04 W1 fix)
      - `grep -qE 'app\.listen.*"pim://quit"' src-tauri/src/lib.rs` — pim://quit listener wired (Plan 05-04 W1 fix)
    - **Plan 05-04 W1 + W2 fix gates:**
      - `grep -qE 'Image::from_path.*tray\.png' src-tauri/src/lib.rs` — tray asset loaded (W2)
      - `grep -q '"icons/tray.png"' src-tauri/tauri.conf.json` — tray asset bundled (W2)
      - `! grep -q "exit as appExit" src/components/tray-popover/popover-actions.tsx` — broken plugin-shell exit import is gone (W1)
      - `! grep -q '@tauri-apps/plugin-shell' src/components/tray-popover/popover-actions.tsx` — popover does not import a JS exit API (W1)
    - **Verbatim-copy gates:**
      - GATE-04 verbatim string present in `src/components/gateway/linux-only-panel.tsx`
      - GATE-04 continuation string present
      - D-09 banner copy present in `src/components/gateway/preflight-section.tsx`
      - D-14 empty-state copy present in `src/components/gateway/peers-through-me-list.tsx`
      - D-34 critical toast copy present in `src/lib/notifications/policy.ts`
    - **Build sanity:**
      - `pnpm typecheck` exits 0
      - `cd src-tauri && cargo check` exits 0
  </acceptance_criteria>
  <done>All TBD-* markers present at expected counts; W1 invariant preserved; verbatim copy-gates pass; typecheck + cargo check green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — all 6 ROADMAP §Phase 5 success criteria + 8 REQ end-to-end</name>
  <files>(none — human-only verification; no files modified)</files>
  <action>This is a human-verify checkpoint. Execute-phase pauses here and resumes on the resume-signal.</action>
  <what-built>
    End-to-end Phase-5 surface across six plans:
    - Plan 05-01: cmdk + plugin-notification + plugin-positioner installed; tauri tray-icon feature flag flipped on; capabilities scoped on the main window; sidebar gateway flipped to ⌘4; ActiveScreenId extended; AppShell binds ⌘4 + ⌘K; placeholder GatewayScreen + useCommandPalette stub.
    - Plan 05-02: GatewayScreen branches on platform; PreflightSection renders the §2a mockup target with humanized check names + recovery hints; NatInterfaceSelect submits gateway.enable; LinuxOnlyPanel renders SETUP-02 verbatim copy on macOS / Windows.
    - Plan 05-03: GatewayActivePanel renders the §2b mockup target with ConntrackGauge (32-char ASCII bar with role=meter), ThroughputPanel (formatBitrate two-line layout), PeersThroughMeList (REUSES Phase 2 PeerRow). useGatewayStatus subscribes to gateway.event via fan-out (or 1Hz polling under TBD-RPC-FALLBACK).
    - Plan 05-04: tray icon + native menu (Linux right-click) + borderless popover window (macOS/Windows left-click) — built upfront in `src-tauri/src/lib.rs` setup hook via helpers in `src-tauri/src/tray.rs`. Capability scoped to tray-popover. LSUIElement absent (window-first preserved). TBD-PHASE-4-A/B/G placeholders rendered in popover React tree.
    - Plan 05-05: cmdk Command.Dialog mounted at AppShell with PALETTE_ACTIONS encoding 17 D-27 verbatim labels; cmdk brand-override CSS appended to globals.css.
    - Plan 05-06: GatewayNotificationsListener subscribes to status.event + peers.event + gateway.event via fan-out, dispatches per-event to silent / toast / system / both per the §8 policy table; lazy permission flow; synthesized all-gateways-lost detection.
  </what-built>
  <how-to-verify>
    **Setup**
    1. Ensure `pim-daemon` is built or fetched (`scripts/fetch-daemon.sh`). For Linux gateway-mode tests you need a real Linux host with iptables.
    2. Run `pnpm tauri dev`. The Tauri dev window opens.
    3. If first-run, complete the Phase 01.1 first-run flow (name device + role), then start the daemon and let the dashboard populate.

    **SC-1: Linux pre-flight check rendering** (GATE-01)

    On a Linux host:
    1. Press `⌘4` (or click sidebar `> gateway`). Gateway tab opens.
    2. Verify the §2a mockup distinctive elements:
       - heading `share your internet with the mesh`
       - sub-heading `pre-flight` (or `pre-flight  · all checks passed` if all ok)
       - one row per check, with `◆` (active green) for ok or `✗` (destructive red) for fail
       - humanized names verbatim per D-04 (`running on linux`, `iptables present`, `CAP_NET_ADMIN available`, `network interfaces detected`)
       - failed rows show daemon detail string + recovery hint (e.g. `· install: sudo apt install iptables`)
    3. To test the failing path: rename `/usr/sbin/iptables` to `/usr/sbin/iptables.bak` (root), click `[ Re-run pre-flight ]`. The iptables row should flip to `✗` with the daemon's detail string + recovery hint. Restore.
    4. **PASS criteria:** every pre-flight check renders honestly; failure hint copyable; re-run works.

    **SC-2: Linux gateway active state** (GATE-03)

    On a Linux host with all checks passing:
    1. From the Gateway tab, select a `nat_interface` from the Select dropdown.
    2. Click `[ Turn on gateway mode ]`. Daemon receives `gateway.enable({ nat_interface: "wlan0" })` (verify via Logs tab — should see a `gateway.enable` RPC line within 1s).
    3. The Gateway tab transitions to the active state (§2b mockup):
       - `◆ gateway active · {nat_interface} · {uptime}` row with the ◆ in signal-green
       - `advertised: 0.0.0.0/0`
       - `conntrack` heading + ASCII bar `[████░░░░] N / max (pct%)` — bar fills with `█` for used, `░` for headroom
       - `throughput` two-line block with `in {x.x MB/s}   out {x.x KB/s}` and `total {Xh Ym}    in {y.y GB}    out {z.z GB}`
       - `peers routing through this node ({n})` heading + filtered peer rows OR empty-state `no peers routing through this node yet · advertising 0.0.0.0/0`
       - `[ Turn off gateway mode ]` action; if peers > 0, advisory `· {n} peers will be cut over to another gateway`
    4. Open ~50 connections through the gateway (e.g. `for i in $(seq 1 50); do curl -s example.com > /dev/null & done`). Observe the conntrack gauge increment within 1s. At ≥ 80% the FILLED portion flips to amber. At ≥ 95% it flips to red AND the parent badge changes from `[ACTIVE]` to `[NEAR LIMIT]`.
    5. Click `[ Turn off gateway mode ]`. Within 2s the panel returns to the pre-flight branch.
    6. **PASS criteria:** all four sub-elements present; gauge color thresholds work; turn-off is one-click + recovers to pre-flight.

    **SC-3: macOS / Windows Linux-only messaging** (GATE-04)

    On a non-Linux host (or by patching `gateway.preflight()` daemon-side to return `platform: "macos"` for testing):
    1. Press `⌘4`. Gateway tab opens.
    2. Verify the §2c mockup verbatim:
       - `Gateway mode is Linux-only today.`
       - `Your device can still join a mesh as a client or relay. Gateway support for macOS and Windows depends on the kernel growing iptables-equivalent NAT — see the kernel repo for status.`
       - `· platform: macos` (or `windows`)
       - `· supported: false`
       - `[ Open kernel repo ]` button
    3. Click `[ Open kernel repo ]` — system browser opens https://github.com/Astervia/proximity-internet-mesh.
    4. CliPanel `[STATUS]` badge reads `LINUX-ONLY`.
    5. Section is RENDERED, not hidden.
    6. **PASS criteria:** verbatim copy present; section visible; button opens repo.

    **SC-4: Tray + popover + native menu parity** (UX-05, UX-06)

    macOS:
    1. The app launches WITH a dock icon visible (LSUIElement is absent — window-first preserved).
    2. The tray icon appears in the menu bar (status area, top-right of macOS).
    3. Left-click the tray icon. Borderless popover appears below the icon (positioned via `tauri-plugin-positioner` `Position::TrayCenter`).
    4. Popover layout (§2d mockup):
       - `◆ pim · {node.name}`
       - `mesh: {mesh_ip}`
       - `[ Route internet via mesh ]` button labeled `(phase 4)` — TBD-PHASE-4-A placeholder; clicking is a no-op (acceptable for Phase 5)
       - `egress: {selected_gateway ?? "local"}` text — TBD-PHASE-4-B fallback selector return
       - `+ Add peer nearby   ⌘⇧N` — clicking emits `pim://open-add-peer`, brings main window forward, navigates to peers tab (Phase 2 Nearby panel)
       - `Open pim   ⌘O` — brings main window forward
       - `Quit pim   ⌘Q`
    5. Click outside the popover — popover hides (D-21 hide-on-blur).

    Linux:
    1. Right-click the AppIndicator (libayatana). Native GTK menu opens (§2e mockup):
       - status header `◆ pim · {name}` (disabled MenuItem)
       - mesh address `mesh: {ip}` (disabled MenuItem)
       - `[✓] Route internet via mesh` (CheckMenuItem — TBD-PHASE-4-A handler is a no-op + tracing log)
       - `Routing — local` disabled MenuItem (TBD-PHASE-4-B fallback)
       - `Add peer nearby` MenuItem
       - `Open pim` MenuItem
       - `Quit pim` MenuItem
    2. Left-click does NOT open anything (Tauri 2 limitation — `show_menu_on_left_click(false)` per D-17). Verify by looking for it: nothing happens.
    3. Click `Open pim` from the menu — main window comes forward.

    Windows:
    1. Left-click the tray icon — popover appears (same React content as macOS).
    2. Right-click the tray icon — native context menu appears (subset: Open pim, Quit).
    3. Both interaction paths reach the same daemon state.

    All three OS surfaces reflect the SAME daemon state (status dot, node name, mesh IP) via per-window `useDaemonState` listener.

    **PASS criteria:** all three OS surfaces offer status + mesh IP + route toggle (placeholder) + add peer + open pim + quit; window-first preserved on macOS; Linux right-click idiom honored; Windows hybrid.

    **SC-5: ⌘K command palette** (UX-07)

    1. From any tab, press `⌘K`. Centered Dialog opens (cmdk Command.Dialog).
    2. Layout (§2f mockup):
       - prompt `> _`
       - `[esc]` hint top-right
       - 5 group headings UPPERCASE letter-spaced: `NAVIGATE`, `ROUTING`, `PEERS`, `GATEWAY`, `LOGS`
       - 17 items total visible by default in registration order
    3. Type `g` — top result is `go to gateway` (cmdk default ranking — registration order tie-breaks `gateway preflight`)
    4. Type `gate` — `go to gateway` followed by `gateway preflight`, `gateway enable (linux)`, `gateway disable (linux)`
    5. Type `split` — `route on  (turn on split-default routing)` matches via the `split-default` keyword (D-26 + RESEARCH §7b)
    6. Press `↑` / `↓` — selection moves; loops at top/bottom (loop prop on Command.Dialog)
    7. Press `Enter` on `go to gateway` — palette closes, ⌘4 navigation occurs.
    8. Open palette again, click `> show routing table` — palette closes, console.warn logs `TBD-PHASE-4-F: routing screen lands in Phase 4 (ROUTE-03)` (open DevTools console to verify).
    9. Click `> add peer nearby` — palette closes, navigates to peers tab AND emits `pim://open-add-peer` (visible in Logs tab if RPC events surface event emit, or via DevTools network inspection).
    10. Visual brand check: no rounded corners on the Dialog; monospace font everywhere; signal-green selected-row text; no shadows; uppercase group headings.

    **PASS criteria:** 17 items present; ranking deterministic; keywords match synonyms; brand styling preserved; TBD-PHASE-4 markers behave as documented.

    **SC-6: Notification policy** (UX-04)

    1. Connect a peer (e.g. trigger a peers.event { kind: "connected" } via another node). Toast fires with `{label ?? short_id} connected` — NO OS notification (verify macOS Notification Center is empty for this event).
    2. Disconnect a peer — silent (no toast, peer row updates).
    3. Cause a gateway failover (e.g. break route to current gateway, daemon switches to another). Toast fires `Failed over to {new} — {old} lost` — NO OS notification.
    4. Engage the kill-switch (route-on + all gateways unreachable). BOTH a toast AND an OS notification fire with `Blocking internet — gateway unreachable. Open pim to fix.` — verify the OS notification in macOS Notification Center / Windows Action Center / Linux notification daemon.
    5. Click the OS notification — main window comes forward (D-35).
    6. **App-launch permission gate:** restart the app. Verify NO permission prompt appears at launch (D-32 lazy permission). Trigger the kill-switch — NOW the permission prompt appears (only on first system / both event).
    7. Disengage kill-switch (restore route) — toast `Internet routing restored.` — no OS notification (D-34 — disengage is non-critical positive outcome).
    8. Saturate conntrack to ≥ 95% — system notification `gateway conntrack saturated — connections will drop.`
    9. Settings → Notifications section: Phase 3's section may already display the policy table read-only IF Plan 03-06 has shipped a consumer of `policy.ts`. If not, this is deferred per D-36 — note in the SUMMARY.

    **PASS criteria:** lifecycle events stay in-app; only the three critical events escalate to OS notifications; lazy permission flow honored; click-to-focus brings main window forward.

    **Additional Phase-5 brand-rule spot checks:**

    - Gateway tab: ASCII conntrack gauge is `█` filled + `░` empty + `[`/`]` brackets — NOT an SVG arc gauge. The bar is exactly 32 characters wide at 100% utilization.
    - Tray popover: monospace; lowercase prose; no rounded corners; no shadows; ├── separators between groups.
    - Command palette: `[cmdk-root]` border is 1px solid; no `border-radius`; group headings UPPERCASE; selected item color = signal-green.
    - Notification copy: every toast / system body ends with a period (declarative voice); no exclamation marks anywhere.
    - macOS dock icon visible (LSUIElement is absent — window-first per STATE.md row 4).
    - `⌘⇧K` does NOT open the palette (modifier guard preserved per D-42 — ⌘⇧K passes through to browser/DevTools).

    **Failure reporting:** if ANY criterion fails:
    - Which criterion (SC-1..SC-6).
    - Observed vs expected.
    - Screenshot / DevTools console error if relevant.
    - Whether failure is cosmetic (Phase-5 ships but a polish PR is needed) or functional (Phase-5 blocker — re-plan a gap-closure plan).

    **Pass signal:** type `approved` or `all 6 criteria pass` to resume.
  </how-to-verify>
  <resume-signal>Type "approved" or describe which success criterion failed</resume-signal>
  <verify>
    <automated>echo "human-verify checkpoint — no automated verification; see how-to-verify above"</automated>
  </verify>
  <done>All 6 ROADMAP §Phase 5 success criteria observably pass in a running `pnpm tauri dev` session against a real `pim-daemon`; any failing criterion is documented before typing approved.</done>
</task>

</tasks>

<verification>
After both tasks land + the human-verify checkpoint passes:

- All TBD-PHASE-4 markers present at expected counts (Task 1 acceptance)
- TBD-RPC + TBD-RPC-FALLBACK markers present
- W1 daemon-event invariant preserved end-to-end
- All 6 ROADMAP Phase-5 success criteria confirmed by the human checkpoint
- All 8 Phase-5 REQ IDs (GATE-01..04, UX-04..07) have user-facing surfaces verified end-to-end
</verification>

<success_criteria>
- All TBD-PHASE-4-A/B/C/D/F/G markers greppable; counts match RESEARCH §4 inventory.
- All TBD-RPC + TBD-RPC-FALLBACK markers greppable.
- W1 daemon-event invariant preserved (only one new listen() — the documented pim://open-add-peer custom-event listener in app-shell.tsx).
- All Phase-5 verbatim-copy gates pass.
- Brand discipline holds across every Phase-5 NEW file.
- Six ROADMAP success criteria observably pass on a real `pim-daemon`.
- Phase-5 ships v1 for Mira.
</success_criteria>

<output>
After completion (including the human-verify resume), create `.planning/phases/05-gateway-mode-system-surfaces/05-07-SUMMARY.md` per template. Include:

- Verbatim summary of each SC-1..SC-6 outcome (pass / partial / fail + notes).
- Audit results: TBD-PHASE-4 letter counts, TBD-RPC count, W1 grep results.
- Cross-plan file manifest (every Phase-5 file modified).
- Any deferred items that surfaced during human-verify (e.g. Phase 3 Settings → Notifications display if Plan 03-06 hasn't yet consumed policy.ts; multi-monitor popover positioner bug; COPY.md audit when Phase 4 ships UX-08).
- Confirmation: did the macOS notification permission prompt fire at app launch (BAD — D-32 lazy violation) or only on the first kill-switch (GOOD)?
- Did `cargo check` + `pnpm typecheck` + `pnpm tauri dev` complete cleanly with no warnings about missing libayatana-appindicator (Linux dev), missing tauri-plugin-positioner / tauri-plugin-notification (any platform)?
- Phase-5 close-out: Mira can ⌘K, Linux user can run gateway, kill-switch escalates to OS — v1 ships.
</output>
