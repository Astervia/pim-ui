{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window — core commands + shell/log plugins.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "log:default"
  ]
}
